{
  "name": "asset-browser",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:manifest": "tsx scripts/build-manifest.ts",
    "build:collision": "tsx scripts/build-collision-mask.ts",
    "dev": "npm run build:manifest && npm run build:collision && vite",
    "build": "npm run build:manifest && npm run build:collision && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "image-size": "^1.1.1",
    "pngjs": "^7.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
